import { api } from '{{pathToApi}}';
import type { {{pascalName}}Props, {{pascalName}}ResponseProps } from '{{pathToTypes}}/{{camelName}}ServiceTypes';

export const {{kebabCase http}}{{pascalName}} = async ({ test }: {{pascalName}}Props): Promise<{{pascalName}}ResponseProps[]> => {
  const environment = import.meta.env.VITE_ENVIRONMENT;

  if (environment === 'DEV') {
    const response = await fetch(`/mock/{{kebabName}}?test=${test}`);
    const data = await response.json();
    return data;
  }
  
  const response = await api.{{kebabCase http}}(`/{{kebabName}}?test=${test}`);
  const data = response.data;
  return data;
};