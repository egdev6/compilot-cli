import { useContext, useMemo, useState } from 'react';
import {{pascalName}}Context from './context';

export const {{pascalName}}Provider = ({ children }) => {
  const [test, setTest] = useState('');

  const value = useMemo(
    () => ({
      test,
      setTest
    }),
    [test]
  );

  return <{{pascalName}}Context.Provider value={value}>{children}</{{pascalName}}Context.Provider>;
};

export const use{{pascalName}} = () => {
  return useContext({{pascalName}}Context);
};
