import { useState } from 'react';
import { {{pascalName}}Props } from './types'

const use{{pascalName}} = (): {{pascalName}}Props => {
 const [test, setTest] = useState<string>('')

  return {
    test,
    setTest
  };
};

export default use{{pascalName}};